{
  "name": "@adidas/product",
  "version": "0.0.1",
  "description": "product micreservice",
  "main": "./lib/app.js",
  "license": "UNLICENSED",
  "scripts": {
    "build": "rm -rf ./lib && npm run build:statics && tsc",
    "clean": "rm -rf ./lib"
  },
  "dependencies": {
    "express": "^4.17.1",
    "node-fetch": "^2.6.0"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}
